export ZSH=$HOME/.oh-my-zsh

ZSH_THEME="{{ zsh.theme }}"
DISABLE_AUTO_UPDATE="true"
#HIST_STAMPS="yyyy/mm/dd"

plugins=(
{% for plugin in zsh.plugins %}
  {{ plugin }}
{% endfor %}
)

{{ shell.aliases }}

{{ shell.exports }}
export PATH="$PATH:{{ user.home }}/.local/bin" # python3 pip packages

source $ZSH/oh-my-zsh.sh
